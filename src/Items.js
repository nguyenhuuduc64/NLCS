export class Item {
    constructor(ten, TL, GT, DG, PA, SL) {
        this.ten = ten; // Tên của đồ vật
        this.TL = TL; // Trọng lượng của đồ vật
        this.GT = GT; // Giá trị của đồ vật
        this.DG = DG;
        this.SL = SL;
        this.PA = PA;
    }
}
